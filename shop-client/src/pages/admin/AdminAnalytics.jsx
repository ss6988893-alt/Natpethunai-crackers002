import { useEffect, useState } from 'react';
import { Bar, BarChart, CartesianGrid, Cell, Legend, Line, LineChart, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { adminApi } from '../../services/api';
const colors = ['#6d4aff', '#f3a712', '#14b8a6', '#ef5b5b', '#3b82f6', '#ec4899']; const money = (value) => `₹${Number(value).toLocaleString('en-IN')}`;
export default function AdminAnalytics() { const [range, setRange] = useState('thisMonth'); const [data, setData] = useState(null); useEffect(() => { adminApi.analytics({ range }).then((r) => setData(r.data)); }, [range]); return <div className="admin-page"><div className="admin-page-title"><div><p>Database analytics</p><h1>Sales analytics</h1></div><select value={range} onChange={(e) => setRange(e.target.value)}><option value="today">Today</option><option value="yesterday">Yesterday</option><option value="last7Days">Last 7 days</option><option value="thisWeek">This week</option><option value="lastWeek">Last week</option><option value="thisMonth">This month</option><option value="lastMonth">Last month</option></select></div>{!data ? <div className="admin-loading">Loading analytics…</div> : <div className="admin-dashboard-grid"><article className="admin-panel admin-chart"><h2>Daily sales</h2><ResponsiveContainer width="100%" height={320}><LineChart data={data.sales}><CartesianGrid stroke="#e8eaf0"/><XAxis dataKey="date"/><YAxis/><Tooltip formatter={(v, n) => n === 'revenue' ? money(v) : v}/><Legend/><Line type="monotone" dataKey="revenue" stroke="#6d4aff" strokeWidth={3}/><Line type="monotone" dataKey="orders" stroke="#f3a712" strokeWidth={2}/></LineChart></ResponsiveContainer></article><article className="admin-panel admin-chart"><h2>Top selling products</h2><ResponsiveContainer width="100%" height={320}><BarChart data={data.topProducts.slice(0, 10)}><CartesianGrid stroke="#e8eaf0" vertical={false}/><XAxis dataKey="name" hide/><YAxis/><Tooltip/><Bar dataKey="quantity" fill="#6d4aff" radius={[8,8,0,0]}/></BarChart></ResponsiveContainer></article><article className="admin-panel admin-chart"><h2>Category-wise revenue</h2><ResponsiveContainer width="100%" height={340}><PieChart><Pie data={data.categories} dataKey="revenue" nameKey="name" innerRadius={70} outerRadius={115} paddingAngle={3}>{data.categories.map((item, i) => <Cell key={item.name} fill={colors[i % colors.length]}/>)}</Pie><Tooltip formatter={(v) => money(v)}/><Legend/></PieChart></ResponsiveContainer></article><article className="admin-panel"><h2>Top products table</h2><div className="admin-table-wrap"><table><thead><tr><th>#</th><th>Product</th><th>Category</th><th>Sold</th><th>Revenue</th><th>Price</th></tr></thead><tbody>{data.topProducts.map((item, i) => <tr key={item._id || item.name}><td>{i + 1}</td><td><strong>{item.name}</strong></td><td>{item.category}</td><td>{item.quantity}</td><td>{money(item.revenue)}</td><td>{money(item.price)}</td></tr>)}</tbody></table></div></article></div>}</div>; }
