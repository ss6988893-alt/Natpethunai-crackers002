import { useEffect, useState } from 'react'; import { adminApi } from '../../services/api';
export default function AdminCustomers() { const [items, setItems] = useState([]); useEffect(() => { adminApi.customers().then((r) => setItems(r.data)); }, []); return <div className="admin-page"><div className="admin-page-title"><div><p>Customer directory</p><h1>Customers</h1></div></div><section className="admin-panel"><div className="admin-table-wrap"><table><thead><tr><th>Customer</th><th>Contact</th><th>Location</th><th>Orders</th><th>Total value</th><th>Last order</th></tr></thead><tbody>{items.map((item) => <tr key={item._id}><td><strong>{item.name}</strong></td><td>{item.mobile}<small className="admin-block">{item.email}</small></td><td>{[item.city,item.district].filter(Boolean).join(', ')}</td><td>{item.orderCount}</td><td>₹{item.totalSpent.toLocaleString('en-IN')}</td><td>{item.lastOrderAt ? new Date(item.lastOrderAt).toLocaleDateString('en-IN') : '—'}</td></tr>)}</tbody></table></div></section></div>; }
