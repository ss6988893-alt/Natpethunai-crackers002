import { memo, useState } from 'react'; import { motion } from 'framer-motion'; import { FiMinus, FiPlus, FiShoppingBag } from 'react-icons/fi'; import { useCart } from '../../context/CartContext';
function ProductCard({ product }){const[quantity,setQuantity]=useState(1);const{addToCart}=useCart();return <motion.article className="product-card" layout initial={{opacity:0,y:20}} animate={{opacity:1,y:0}}><div className="product-card__image"><img src={product.image} alt={product.name} loading="lazy"/><span>-{product.discount}%</span></div><div className="product-card__body"><p>{product.category}</p><h3>{product.name}</h3><div className="price"><strong>₹{product.price.toLocaleString('en-IN')}</strong><del>₹{product.originalPrice.toLocaleString('en-IN')}</del></div><div className="product-card__actions"><div className="quantity"><button onClick={()=>setQuantity(Math.max(1,quantity-1))} aria-label="Decrease quantity"><FiMinus/></button><span>{quantity}</span><button onClick={()=>setQuantity(quantity+1)} aria-label="Increase quantity"><FiPlus/></button></div><button className="add-button" onClick={()=>addToCart(product,quantity)}><FiShoppingBag/> Add</button></div></div></motion.article>}
export default memo(ProductCard);
